# ==================== TUS (Resumable Upload) Server Configuration ====================
# Production Configuration - Update these values for your environment

# Server Configuration
PORT=4001
HOST=0.0.0.0
TUS_PATH=/files
TUS_STORAGE_DIR=/var/tus/data

# Upload Configuration - 20GB default limit
MAX_UPLOAD_SIZE_BYTES=21474836480

# ==================== MinIO (S3 Storage) Configuration ====================
# Configure these for your production MinIO/S3 service
MINIO_ENDPOINT=minio.example.com
MINIO_PORT=9000
MINIO_USE_SSL=true
MINIO_ACCESS_KEY=your_access_key
MINIO_SECRET_KEY=your_secret_key
MINIO_BUCKET=wedocs
