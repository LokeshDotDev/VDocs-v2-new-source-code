# ==================== TUS (Resumable Upload) Server Configuration ====================
# This service handles resumable file uploads using the TUS protocol
# All ports and service endpoints are configurable via environment variables

# Server Configuration
PORT=3001
TUS_PATH=/files
TUS_STORAGE_DIR=./.data/tus

# Upload Configuration - 20GB default limit
MAX_UPLOAD_SIZE_BYTES=21474836480

# ==================== MinIO (S3 Storage) Configuration ====================
# MinIO stores uploaded files after processing
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_USE_SSL=false
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=wedocs
